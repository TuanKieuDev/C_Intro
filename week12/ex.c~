#include <stdio.h>

int addNumbers(int *fiveNumber){
  int i,sum=0;
  for(i=0;i<5;i++;fiveNumber++){
    sum+=*fiveNumber
      }
  return sum;
}

int main()
{
  int a[100];
  printf("Tong cua 5 so la: %d\n",addNumbers(a));
  return 0;
}
