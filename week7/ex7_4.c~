#include <stdio.h>
#include <math.h>
int main()
{
  int i,j;
  int k=0;
  for(i=2;i<=100;i++){
    for(j=2;j<=sqrt(i);j++){
      if(i%j==0) k++;
    }
    if(k==1) printf("%5d",i);
  }
  return 0;
}
